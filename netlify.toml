[build]
  command = "hugo --gc --minify"
  publish = "public"

[dev]
  command = "hugo server --gc --minify"
  publish = "public"

[build.environment]
  HUGO_VERSION        = "0.92.2"
  HUGO_ENABLEGITINFO  = "true"
